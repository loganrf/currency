
class currency():
	"""docstring for currency"""
	
	def __init__(self, name, country, code, symbol, before=True):
		self.name = name
		self.country = country
		self.code = code
		self.symbol = symbol
		self.before = before

		
		